<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heybooks.sh.mybatis.mapper">
	<!-- 관리자 로그인 -->
	<select id="admin_info" parameterType="hashmap" resultType="com.heybooks.sh.vo.Admin_Login_Vo">
	  select * from admins where admin_id = #{admin_id} 
	</select>
</mapper>





















